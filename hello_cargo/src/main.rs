fn main() {
    println!("Hello, world!");
}

/**
 * Create new project using --> cargo new 
 * Build a project using --> cargo build
 * Build and run a project in one step using --> cargo run
 * Build project without producing executable to check for errors --> cargo check 
 * Cargo stores resulting executable in --> target/debug
 * 
 */
